{
	"dependencies": {
		"ejs": "^2.5.7",
		"express": "^4.16.2",
		"react": "^16.2.0",
		"request": "^2.83.0",
		"request-promise": "^4.2.2"
	},
	"scripts": {
		"build": "rm -rf /node_modules && npm install",
		"start": "npm run build && node server.js"
	}
}
